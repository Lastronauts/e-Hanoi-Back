type Query {
  getUser: User!
  listUser(name: String!, start: Int = 0, range: Int = 20): [User!]!
}

type Mutation {
  createUserInDB(newUser: NewUser!): User!
  deleteUserInDB: User!
}

type User {
  id: ID!
  name: String!
}

input NewUser {
  name: String!
}
